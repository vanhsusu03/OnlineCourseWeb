const db = require('../models/');
const Payment = db.payment;

class PaymentController {
    async getTransferInfo(req, res, next) {

    }

    async payForOrder(req, res, next) {

    }
}

module.exports = new PaymentController();